/// @func state_immediate_transition_to(stateIndex)
/// @desc Transition to a state immediately
/// @arg {int} stateIndex
var stateIndex = argument0;

state_transition_to(stateIndex);
state_machine_handle_transition();
