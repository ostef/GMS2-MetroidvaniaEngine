/// @func animation_sequence_is_playing(animSequenceIndex)
/// @desc Returns true if an animation sequence is playing, false if not
/// @arg {int} animSequenceIndex
function animation_sequence_is_playing(argument0) {
	var animSequenceIndex = argument0;

	return animationSequenceIndex == animSequenceIndex;



}
